/**
 * 
 * Main is meant to start the program and execute the various classes associated with TRS capabilities. 
 * SecurityTool is just a CLI tool which lets you perform various capabilities that I am going to outline here.
 * 
 */
/**
 * This needs to include:
 * a -help option, which displays each of the hashing options 
 * including -MD5, -SHA256, -PBKDF2, each with a --hex (or just -h) and --base64 (or just -b).
 * As you may guess, each of those requires its own class. each option is just another addition
 * to each class.
 * 
 * Here's the structure of the program: 
 * TRSSecurityPractices (Program name as of now)
 * |
 * 
 */
package Main;